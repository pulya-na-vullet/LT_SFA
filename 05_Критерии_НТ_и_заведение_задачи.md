# Критерии НТ и заведение задачи

**Название статьи в Confluence:** Критерии НТ и заведение задачи  
**Родительская страница:** НТ пререквизиты NCINS в SFA  
**Метки (labels):** НТ, ЕОС.ЮЛ, NCINS, SFA  
**Версия:** 0.2 от 16.09.2026

> В редакторе Confluence сразу после вставки добавьте макрос **Table of Contents** (оглавление).

---

**Модуль:** NCINS в SFA (`ufr-eos-ul-ncins`)  
Версия: 0.2  
Дата: 16.09.2026

Источник: шаблон «Передача модуля на нагрузочное тестирование» v12,
разделы 1, 2, 3, 11, 12, 13, 14.1, 15.3.1.

---

## 1. Критерии необходимости проведения НТ

### НТ не требуется, если изменились только:

1. Документация.
2. Тестирование.
3. Понижение уровня логирования (trace → debug → error → fatal).
4. Интерфейс без изменения логики метода.
5. Ролевая модель — если это не меняет нагрузку на модули.
6. Удаление метода микросервиса (скрипты НТ всё равно нужно актуализировать).
7. Уменьшение нагрузки на существующий функционал (новые цифры всё равно отдать НТ).
8. Интенсивность метода **1 и менее раз в час в пике** (в т.ч. через 3 года), без пакетных выгрузок
   и массовых обработок.

### НТ требуется

**Новый функционал:**

1. Интенсивность метода **более 1 раза в час в пике** (в т.ч. через 3 года).
2. Пакетные выгрузки и массовые обработки — независимо от интенсивности.

**Существующий функционал (доработки):**

1. Новые обращения к внешним системам или БД.
2. Рост обращений к модулю/БД сверх запаса с последнего ТПМП.
3. Изменение RAM/CPU контейнеров или дата-кластеров.
4. Изменение heap, metaspace, thread pool и т.п.
5. Переключение / подключение / учащение вызовов веб-сервисов шины.
6. Добавление пользователей.
7. Обновление версий ПО и внутренних библиотек.
8. Расширение логирования.
9. Изменение архитектуры: число инстансов, характеристики серверов.
10. Маскирование данных.
11. Сравнительный анализ при смене оборудования.
12. НТ не проводилось более 3 месяцев (чеклист ЕОС.ЮЛ, раздел 15.3.1 шаблона).

Окончательное решение принимает **команда НТ** по задаче в Jira, а не автор анкеты.

---

## 2. Оценка для NCINS (без подмены решения НТ)

Известные факты:

| Факт | Отношение к критериям |
|---|---|
| База **≈89/час, пик ≈180 заявок/час** | Для бизнес-потока заявок критерий «> 1/час» выполняется |
| `core-api`: `log-payload: true`, `pretty-print: true` | Попадает под «расширение логирования», **если это изменение относительно прошлого НТ/прома** |
| 2 реплики, JVM/лимиты как в helm | Это **готовность стенда** (раздел 12 шаблона), а не самостоятельное доказательство, что НТ модуля нужно |
| Отклонения JVM vs «50% Request / limit +20%» | Чинить/согласовывать до боя; не путать с критерием «нужно ли НТ» |
| Проводилось ли НТ NCINS ранее | `_TBD_` |

Предварительно: для **нового** модуля NCINS в SFA с ЧПН ≫ 1/час постановка на НТ **оправдана**.
Формулировка «НТ требуется, потому что Xmx gateway 70%» — некорректна.

---

## 3. Критерии конфигурации модулей ЕОС.ЮЛ (раздел 12 шаблона)

1. Мониторинг JVM — срок середина Q1.
2. `-Xms` = `-Xmx` = **50% от Request** памяти пода.
3. Заданы `-Xss` и `-XX:MaxMetaspaceSize`.
4. Limit памяти = Request **+20%**.
5. `-XX:+PrintFlagsFinal`.
6. `jcmd` в контейнере — пока не надо.
7. Утилизация CPU/памяти на НТ — **не более 80% от Request**.

Проверка по подам NCINS — в статье «Готовность тестовой среды».

---

## 4. Заведение задачи

1. Открыть родительскую задачу **«SFA ЮЛ ТЕСТИРОВАНИЕ РЕГРЕССИОННОЕ»**.
   В PDF шаблона ключ обрезан (`LOADTEST-2613` в сноске,
   `LOADTEST-261387` в тексте). Актуальный ключ взять с доски
   https://jira.moscow.alfaintra.net/secure/RapidBoard.jspa?rapidView=60242
2. Create → Issue Type **Regular task**.
3. Тип запроса:
   - «Проведение первичного НТ» — новый модуль;
   - «Проведение регулярного НТ» — доработка.
4. Summary — наименование модуля (`ufr-eos-ul-ncins` / «Страховки для бизнеса»).
5. Description — текстом, не ссылкой:
   - 3–4 предложения: зачем модуль и кто пользуется;
   - цели НТ (макс. нагрузка / соответствие требованиям);
   - дата предполагаемого релиза в прод.
6. Assignee — менеджер по производительности.
7. «Система в НТ» — **SFA ЮЛ**.
8. label — **`НТ_ЕОС_ЮЛ`**.
9. Плановая дата начала — когда будут готовы стенд и анкета.
10. Финальный статус: **cancel** если НТ не нужно, **done** если нужно.

Дальше по шаблону:

1. Письмо Веретенникову Сергею Александровичу со ссылкой на задачу.
2. Решение команды НТ.
3. Если НТ нужно — дозаполнить постановку (разделы 2–3 анкеты, заглушки, сценарии).
4. Выделить разработку и QA на профиль, сценарии и моки.

---

## 5. Стенд НТ SFA ЮЛ (раздел 14.1 шаблона)

План 2025 (подписка AI-4031, VM, k8s, мониторинг, ELK) — исторический.  
Актуальный статус на 2026 — у инженера НТ.

По этому репозиторию целевой контур: кластер `ufrulkltmaster1.moscow.alfaintra.net`,
namespace в values `ufr-eos-ul-ncins` (в README указан другой — `ufr-ncins`).

---

## 6. Запросить у НТ

- [ ] Актуальный ключ родительской Jira-задачи
- [ ] Статус стенда SFA ЮЛ на 2026
- [ ] Grafana по `ufr-eos-ul-ncins`
- [ ] Что на ЛТ живое, что мок (UI уже смотрит в `ufrulltmock1/2`)
- [ ] Kafka: реальный брокер или нет
- [ ] Формат дата-пулов
- [ ] Согласование отклонений JVM/лимитов (см. «Готовность тестовой среды»)