# НТ пререквизиты NCINS в SFA

**Название статьи в Confluence:** НТ пререквизиты NCINS в SFA  
**Родительская страница:** (корневая в пространстве, например UP / ЕОС.ЮЛ)  
**Метки (labels):** НТ, ЕОС.ЮЛ, NCINS, SFA  
**Версия:** 0.2 от 16.09.2026

> В редакторе Confluence сразу после вставки добавьте макрос **Table of Contents** (оглавление).

---

Этот комплект — постановка модуля **некредитного страхования ЮЛ в SFA / ЕОС.ЮЛ** (`ufr-eos-ul-ncins`, UI «Страховки для бизнеса») на нагрузочное тестирование.

Это **не** модуль «Нефинансовые сервисы ЮЛ» (NSSFA) и **не** платформа SFA ЮЛ целиком.

Форма — **одна анкета для входа по модулю** (как в шаблоне «Передача модуля на нагрузочное тестирование»). Отдельные постановки на каждый k8s-сервис не делаются: `gateway-router` и `access-checker` — инфраструктура объекта тестирования, а не самостоятельные сценарии НТ.

`_TBD_` означает: значения нет в исходниках. Его нельзя подменять экспертной оценкой в колонке «Значение».

## Дерево статей

Создайте эту страницу как родителя. Остальные шесть — **дочерние** (Create → страница → Parent = эта).

| Порядок | Название дочерней статьи | Что внутри |
|---|---|---|
| 1 | Анкета для входа NCINS в SFA | объект, пререквизиты, профиль UC, стык с UMP |
| 2 | Заглушки и интеграции | внешние системы, что мокать |
| 3 | Готовность тестовой среды | стенд ЛТ, JVM, точки наблюдения |
| 4 | Критерии НТ и заведение задачи | критерии, Jira, письмо НТ |
| 5 | База расчёта нагрузки | ЧПН 15 000/мес, без выдуманных ×N |
| 6 | Состав сервисов | паспорт подов, не объекты НТ |

После создания дочерних страниц вставьте на эту страницу макрос **Children Display**.

## Как загрузить в Confluence

1. Создайте родительскую страницу с названием **НТ пререквизиты NCINS в SFA**.
2. Для каждого файла из архива: Create child page → название как в таблице выше → вставьте содержимое файла (начиная с первого заголовка).
3. Если в редакторе есть *Insert → Markup / Markdown* — вставляйте как Markdown. Иначе: визуальный редактор, таблицы копируются из Markdown-превью (GitHub/VS Code) или через Word.
4. Добавьте макрос Table of Contents на каждую статью.
5. Проставьте метки: `НТ`, `ЕОС.ЮЛ`, `NCINS`, `SFA`.

Секреты и пароли в статьи **не** выносить.

## Назначение комплекта

Структура разделов пререквизитов сохранена по эталону UMP (`НТ пререквизиты - Worker ump-*-ncins-pa`).

## Источники

- helm-values и configMap модуля `ufr-eos-ul-ncins-*`;
- 6 документов UMP «НТ пререквизиты - Worker ump-*-ncins-pa»;
- шаблон «Передача модуля на нагрузочное тестирование» v12;
- пример заведения задачи НТ ЕОС.ЮЛ;
- анкета NSSFA — **только как образец формы**, не как данные NCINS.

Не использованы как факты: догадки о числе кликов/методов на заявку, SLA UMP-процессов как SLA HTTP SFA.

## Что нужно, чтобы закрыть `_TBD_`

| # | Источник | Закрывает |
|---|---|---|
| 1 | Git `ufr-eos-ul-ncins-core-api` и `ufr-eos-ul-ncins-ui` (проект NCINS, не NSSFA) | алгоритмы, OpenAPI, payloads |
| 2 | Swagger стенда `GET /ufr-eos-ul-ncins-gateway-router-api/ufr-eos-ul-ncins-core-api/v3/api-docs` | контракты REST |
| 3 | Confluence БП NCINS в SFA | пользовательские UC |
| 4 | Аналитик | SLA, ЧПН по методам, прогноз +6 мес, сценарии |
| 5 | Инженер НТ | что на ЛТ живое / мок, Grafana, Kafka |
| 6 | Согласующие | ФИО в блоке согласования |

Раздел 1 анкеты достаточен для решения о необходимости НТ. Разделы «полная информация» и таблица методов заполняются после заключения инженера НТ.
