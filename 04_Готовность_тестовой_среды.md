# Готовность тестовой среды

**Название статьи в Confluence:** Готовность тестовой среды  
**Родительская страница:** НТ пререквизиты NCINS в SFA  
**Метки (labels):** НТ, ЕОС.ЮЛ, NCINS, SFA  
**Версия:** 0.2 от 16.09.2026

> В редакторе Confluence сразу после вставки добавьте макрос **Table of Contents** (оглавление).

---

**Модуль:** NCINS в SFA (`ufr-eos-ul-ncins`)  
Версия: 0.2  
Дата: 16.09.2026

Шаблон: п. 5.6–5.8, 14.1.

---

## Контур

| Параметр | Значение |
|---|---|
| Namespace (values) | `ufr-eos-ul-ncins` |
| Namespace (README) | `ufr-ncins` — **расхождение, верить values** |
| Кластер | `ufrulkltmaster1.moscow.alfaintra.net` |
| Профиль | `loading` |
| Реестр | `eco.binary.alfabank.ru` |
| API | `lt.ufrulklt-api.moscow.alfaintra.net` |
| UI | `lt.ufrulklt-ui.moscow.alfaintra.net` |

Стенд НТ ≈ прод? **Нет, не утверждать.** Config смешанный:

| Слой | Куда смотрит |
|---|---|
| Redis | LT (`ufrulltredis1..6`) |
| Kafka | фиксированные IP |
| UMP / NIB / Keycloak UMP | `*test` (`umptest`, `corp-gateway-test`, `idp-api-test`) |
| UFR accounts/customers/roles/session/sign | DNS `*.ufr-eos-ul-*-int.svc.cluster.local` (**INT-namespace**) |
| UI session/roles/keycloak | `ufrulltmock1:8110`, `ufrulltmock2:8188` |
| `module-info` `module.host` | `int.ufrulkint-ui...` (**INT UI**, не `lt.ufrulklt-ui`) |

Архитектура 1:1 с промом и совпадение версий ПО — `_TBD_`.

---

## Состав на стенде

| Сервис | Образ | Реплик | Health | Prometheus |
|---|---|---|---|---|
| `ufr-eos-ul-ncins-core-api` | `ufr-eos-ul-ncins-core-api` | 2 | `/admin/health` (+ liveness/readiness) | `/admin/prometheus:8081` |
| `ufr-eos-ul-ncins-gateway-router-api` | `ufr-eos-gateway-router-api` | 2 | `/admin/health` | `/actuator/prometheus:8081` |
| `ufr-eos-ul-ncins-module-info-api` | `ufr-eos-module-info-api` | 2 | `/actuator/health` | `/actuator/prometheus:8081` |
| `ufr-eos-ul-ncins-access-checker` | `ufr-access-checker` | 2 | `/admin/health` | `/admin/prometheus:8081` |
| `ufr-eos-ul-ncins-ui` | `ufr-eos-ul-ncins-ui:1.0.1` | 2 | `/healthmonitor` | нет в values |
| `ufr-eos-ul-ncins-settings` | — | — | — | в README есть, **файла values в репо нет** |

UI: порт контейнера в values **не объявлен** (задаётся в `start.sh`) — уточнить у разработки.  
Access-checker в README install **не указан**, values есть.  
README ставит `gateway` / `virtualservice` / secrets — этих values в репо нет.

Вне контура модуля: Redis ×6, Kafka, два Keycloak, UFR common-services / document-processing / client-session.

---

## Ресурсы и JVM

| Сервис | CPU req/lim | Mem req/lim | Heap |
|---|---|---|---|
| core-api | 90m / 240m | 1024Mi / 1228Mi | `-Xms512M -Xmx512M -XX:MaxMetaspaceSize=160M` |
| access-checker | 100m / 500m | 1024Mi / 1536Mi | `-Xms512M -Xmx512M -Xss1024K -XX:MaxMetaspaceSize=128M` |
| gateway-router | 10m / 240m | 614Mi / 740Mi | `-Xms430M -Xmx430M -XX:MaxMetaspaceSize=128M` |
| module-info | 10m / 240m | 614Mi / 740Mi | `-Xms430M -Xmx430M -XX:MaxMetaspaceSize=128M` |
| ui | 100m / 1000m | 256Mi / 512Mi | Node.js |

Критерии ЕОС.ЮЛ (раздел 12 шаблона):

| Критерий | core-api | access-checker | gateway | module-info | ui |
|---|---|---|---|---|---|
| `-Xms`=`-Xmx`=50% Request | да, 512/1024 | да | **нет, 430/614 ≈ 70%** | **нет, 70%** | n/a |
| `-Xss` и MaxMetaspace | Metaspace да, `-Xss` нет | оба | Metaspace да, `-Xss` нет | то же | n/a |
| Limit mem = Request+20% | да (1228) | **нет, +50% (1536)** | да | да | **нет, +100%** |
| `PrintFlagsFinal` | да | да | да | да | n/a |
| Дамп при OOM | HeapDump | **`ExitOnOutOfMemoryError`, дампа нет** | нет явного HeapDump | нет | n/a |

Пустой `-Djavax.net.ssl.trustStorePassword=` — часто штатно для JKS, не считать дефектом без подтверждения.

До боя согласовать с НТ: heap gateway/module-info, limit access-checker/UI, OOM-поведение access-checker, CPU limit UI ×10 от request.

---

## Точки наблюдения

| Метрика | Где | Статус |
|---|---|---|
| HTTP, ошибки | Prometheus сервисов | endpoint есть; у core-api включён `percentiles-histogram` `http.server.requests` |
| Health зависимостей | access-checker | сервис есть, в профиль нагрузки не включать |
| Очереди Kafka | брокер / Grafana | `_TBD_` |
| JVM heap/GC/threads | Grafana | ссылка `_TBD_` |
| Бизнес-операции (заявка/подпись/договор) | `_TBD_` | в конфиге нет готовых бизнес-метрик |

Предполагаемые узкие места (гипотезы для плана НТ, не «найденные дефекты»):

1. OBIP (синхронный PDF).
2. Redis при `failover-cache: false`.
3. Kafka producer `acks=all` + idempotence.
4. Полное HTTP-логирование payload в core-api → диск/ELK.
5. Feign retry ×3 при деградации NIB/UMP.
6. `client-session` — SPOF для `/core-api/**`.
7. UI CPU limit 1000m при request 100m.

---

## Тестовая модель

Объём клиентов/счетов/полисов, генерация дата-пулов — `_TBD_` (аналитик).

Известные ограничения данных из config:

| Ограничение | Значение | Не путать с |
|---|---|---|
| Роль | `NCINS_UL_ACCESS` | — |
| Уровень сессии | `1` | — |
| Счета | `RUR` + `CA` + бухрежим `100` | — |
| `xpin-const` | `XAAAAA` | это не PIN клиента в дата-пуле |
| Канал NIB-клиента SFA | `sfa` / project `ncins` | — |
| Номер договора (UMP) | маска `Z6922/888/ABR*****/6` | если SFA генерирует номер так же — подтвердить |

---

## Развёртывание

Подмодуль чарта в этом git не выкачан. README:

```shell
git submodule update --init --recursive
git submodule update --rebase --remote
```

Команды README рассчитаны на namespace `ufr-ncins` и файлы, которых нет
(`ufr-eos-ul-ncins-settings.yml`, `spring-application-gateway.yml`, `values-gateway.yml`).
Перед боем актуализировать install под фактические values.

Ориентир по имеющимся файлам (namespace как в values):

```shell
helm upgrade --install -n ufr-eos-ul-ncins ufr-eos-ul-ncins-core-api       ufr-chart/ -f ufr-eos-ul-ncins-core-api.yml
helm upgrade --install -n ufr-eos-ul-ncins ufr-eos-ul-ncins-access-checker ufr-chart/ -f ufr-access-checker.yml
helm upgrade --install -n ufr-eos-ul-ncins ufr-eos-ul-ncins-gateway        ufr-chart/ -f ufr-eos-gateway-router-api.yml
helm upgrade --install -n ufr-eos-ul-ncins ufr-eos-ul-ncins-module-info-api ufr-chart/ -f ufr-eos-module-info-api.yml
helm upgrade --install -n ufr-eos-ul-ncins ufr-eos-ul-ncins-ui             ufr-chart/ -f ufr-eos-ul-ncins-ui.yml
```